  <nav>{{> logout}}</nav>
<h3>Signers from {{city}}</h3>
<h2>We the undersigned demand that driving private Cars will be forbidden in Berlin!</h2>
{{#rows}}  
  <div class ="container">
       <p class="ListNames">{{first}} {{last}} {{age}}<a href="{{url}}">{{url}}</a></p>
       <div>
  {{/rows}}
